Когда начал работать над этим этапом, захотел переписать нормально AGENTS.md и README.md. В процессе понял, что хочу вообще другого, нежели изначально было заявлено (не классическую систему для хранения переводов с экспортом/импортом, и прям интерфейс к YAML-файлам в гите, чтобы поддерживать работу с бранчами/PR, версионность, мерджи и прочее). В итоге полностью удалил весь код, с ГПТ сформировал описание нового проекта, сократил его, обновил файлы.

Из полученного AGENTS.md сгенерерировал бэклог с минимальными атомарными шагами, через агента создал issues на Гитхабе.

Далее отчет по каждой фиче отдельно.

## Feature 002: Establish initial application entry point and operability check

Бриф родился со второго раза, сначала агент родил скорее спеку с AC и прочим (хотя я довел ее до 0 замечаний).

На второй итерации сразу попросил агента быть кратким и перечислил желаемые секции. Также сразу попроосил добавить front matter (тут очевидно нужны шаблоны).

Формирование брифа (обе попытки) заняли примерно 1 час. Еще 25 минут ушло на спеку. План делать не стал, слишком простая фича.

Реализация заняла 2-3 минуты вместе с ручной проверкой.

Какие замечания:
- с первого раза вместо брифа получилась скорее спека. Для решения проблемы скорректировал промпт (попросил быть короче, сфокусироваться на проблеме, а не решении и перечислил желаемые секции)
- нужны шаблоны брифа/спеки, чтобы не раздувать промпты и не дублировать каждый раз одно и то же

## Feature 004: Loki lacks a way for users to register repositories

Здесь все прошло быстрее, бриф и спека заняли примерно час суммарно.

В этой фиче попробовал сделать план реализации (объективно он был не нужен, но просто для обкатки процесса).

Реализация заняла так же 2-3 минуты. В коде увидел использование конвенций, которые мне не нравятся, доработал конфиг Рубокопа и AGENTS.md (через агентов).

Понял, что в прошлой фиче забыл сделать ревью кода, в этот раз сделал, с подготовкой промптов и чтением отчетов заняло примерно минут 20-30.

Ревью выдало confidence 0.82 из-за того, что в сендбоксе не смогло проверить поведение (запустить тесты).

Замечания:
- настроил Codex и доступ к БД, чтобы агент мог запускать тесты в сендбоксе.
