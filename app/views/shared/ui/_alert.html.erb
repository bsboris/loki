<% raise ArgumentError, "Unknown alert variant: #{variant.inspect}. Must be one of [:info, :error]" unless %i[info error].include?(variant) %>
<% alert_classes = ["alert", "alert-#{variant}"] %>

<div class="<%= alert_classes.join(" ") %>" role="alert">
  <span><%= message %></span>
</div>
